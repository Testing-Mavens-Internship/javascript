let x = 2;

switch (x) {
    case 1: console.log("number found "+ x);
        break;
    case 2: console.log("number found "+ x);
        break;
    case 3: console.log("number found "+ x);
        break;
    case 4: console.log("number found "+ x);
        break;
    default: console.log("Wrong number!!!");
        break;
}